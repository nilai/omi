# Omi 工程详细介绍

## 一、功能概述

Omi 是一个开源的AI可穿戴设备生态系统，主要功能包括：

### 1.1 核心功能

#### 音频捕获与转录
- **实时音频流处理**：通过蓝牙BLE连接可穿戴设备，实时接收音频数据
- **多格式音频解码**：支持AAC、Opus、LC3等多种音频编码格式
- **语音活动检测（VAD）**：使用Silero VAD进行语音端点检测
- **实时语音转文字（STT）**：集成Deepgram、Soniox、Speechmatics等STT服务
- **多语言支持**：支持多种语言的实时转录和翻译

#### 对话处理与分析
- **对话结构化**：自动提取对话中的关键信息
  - 行动项（Action Items）提取
  - 事件（Events）识别
  - 人物识别与标注
  - 摘要生成
- **智能记忆管理**：使用向量数据库（Pinecone）存储和检索对话记忆
- **RAG检索增强**：基于主题和时间的智能记忆检索
- **对话分类**：自动对对话进行分类和标签化

#### AI聊天与交互
- **个性化AI助手**：基于用户记忆的上下文感知对话
- **插件系统**：支持第三方应用集成（Apps/Plugins）
- **Persona系统**：可创建自定义AI角色和人格
- **实时流式响应**：支持流式输出AI回复

#### 任务管理集成
- **行动项管理**：自动提取和管理待办事项
- **第三方集成**：支持Todoist、Asana、ClickUp、Google Tasks、Apple Reminders等
- **任务同步**：跨平台任务同步和提醒

#### 设备管理
- **多设备支持**：支持Omi设备、Apple Watch、Note设备等
- **设备发现与连接**：蓝牙设备自动发现和连接管理
- **固件更新（OTA）**：支持设备固件无线更新
- **设备状态监控**：电池、连接状态等实时监控

### 1.2 扩展功能

- **照片处理**：对话中照片的自动描述和分析
- **位置信息**：自动提取对话中的地理位置信息
- **趋势分析**：分析用户对话中的趋势和模式
- **通知系统**：智能推送和提醒
- **支付集成**：Stripe支付集成，支持订阅管理
- **开发者API**：提供API密钥管理和开发者工具

---

## 二、系统流程

### 2.1 音频捕获流程

```
设备端（Omi硬件）
    ↓ [蓝牙BLE]
移动端App（Flutter）
    ↓ [WebSocket]
后端服务（FastAPI）
    ↓ [STT服务]
转录结果
    ↓ [实时推送]
移动端App显示
```

**详细步骤：**

1. **设备发现与连接**
   - App启动设备扫描服务（`DeviceService`）
   - 通过蓝牙BLE发现Omi设备
   - 建立BLE连接，订阅音频特征值

2. **音频数据流**
   - 设备通过BLE发送编码后的音频数据包
   - App接收数据并解码（AAC/Opus/LC3）
   - 通过WebSocket实时发送到后端

3. **后端处理**
   - 接收音频流，进行VAD检测
   - 将音频发送到STT服务（Deepgram/Soniox）
   - 实时返回转录文本片段

4. **实时显示**
   - 后端通过WebSocket推送转录片段
   - App实时更新UI显示转录内容

### 2.2 对话处理流程

```
转录完成
    ↓
创建对话记录（状态：processing）
    ↓
并行处理：
    ├─ 提取结构化信息（行动项、事件、人物）
    ├─ 生成摘要
    ├─ 提取记忆（Memories）
    ├─ 向量化存储
    └─ 触发插件/应用
    ↓
更新对话状态（completed）
    ↓
通知用户
```

**详细步骤：**

1. **对话结束**
   - 用户停止录音或达到时间限制
   - 后端标记对话为"processing"状态

2. **并行处理任务**
   - **结构化提取**：使用LLM提取行动项、事件、人物
   - **摘要生成**：生成对话摘要
   - **记忆提取**：识别并提取重要记忆点
   - **向量化**：将记忆存储到Pinecone向量数据库
   - **插件触发**：触发已安装的第三方应用

3. **结果存储**
   - 更新Firestore数据库
   - 更新Redis缓存
   - 发送完成通知

### 2.3 AI聊天流程

```
用户输入问题
    ↓
判断是否需要上下文
    ↓
如果需要：
    ├─ 提取问题主题
    ├─ 向量检索相关记忆
    ├─ 提取对话片段
    └─ 构建上下文
    ↓
调用LLM（OpenAI/Anthropic）
    ↓
流式返回响应
    ↓
更新对话历史
```

**详细步骤：**

1. **问题分析**
   - 判断是否需要上下文（`requires_context`）
   - 提取问题主题和日期范围

2. **上下文检索**
   - 使用向量数据库检索相关记忆
   - 提取最相关的对话片段
   - 构建上下文提示词

3. **AI生成**
   - 调用LLM API（支持流式输出）
   - 实时返回生成的文本

4. **结果处理**
   - 更新聊天历史
   - 可能触发新的记忆提取

### 2.4 设备同步流程（WAL）

```
设备端（离线存储）
    ↓ [连接时]
移动端App
    ↓ [WAL协议]
后端服务
    ↓
处理并存储对话
```

**WAL（Write-Ahead Log）同步机制：**
- 设备离线时在本地存储对话数据
- 连接时通过WAL协议同步到后端
- 支持断点续传和冲突解决

---

## 三、模块架构

### 3.1 前端应用（Flutter）

#### 3.1.1 目录结构

```
app/lib/
├── main.dart                    # 应用入口
├── core/                        # 核心模块
│   └── app_shell.dart          # 应用外壳
├── pages/                       # 页面模块
│   ├── home/                   # 首页
│   ├── conversations/          # 对话列表
│   ├── conversation_detail/    # 对话详情
│   ├── chat/                   # AI聊天
│   ├── memories/               # 记忆管理
│   ├── action_items/           # 行动项
│   ├── apps/                   # 应用/插件管理
│   ├── settings/               # 设置
│   └── onboarding/            #  onboarding流程
├── providers/                   # 状态管理（Provider模式）
│   ├── capture_provider.dart   # 音频捕获
│   ├── conversation_provider.dart
│   ├── device_provider.dart    # 设备管理
│   ├── message_provider.dart   # 消息管理
│   └── ...
├── services/                    # 服务层
│   ├── devices/                # 设备服务
│   │   ├── device_connection.dart
│   │   └── discovery/          # 设备发现
│   ├── sockets/                # WebSocket连接
│   │   ├── transcription_connection.dart
│   │   └── wal_connection.dart
│   ├── wals.dart               # WAL同步服务
│   └── ...
├── backend/                     # 后端API封装
│   ├── http/                   # HTTP客户端
│   └── schema/                 # 数据模型
├── ui/                          # UI组件
│   ├── atoms/                  # 原子组件
│   ├── molecules/              # 分子组件
│   └── organisms/              # 有机体组件
└── utils/                       # 工具类
```

#### 3.1.2 核心模块说明

**Providers（状态管理）**
- `CaptureProvider`：管理音频捕获状态、WebSocket连接、转录流
- `DeviceProvider`：管理设备连接、发现、状态
- `ConversationProvider`：管理对话列表、CRUD操作
- `MessageProvider`：管理消息和事件
- `MemoriesProvider`：管理记忆数据
- `AppProvider`：管理第三方应用/插件

**Services（服务层）**
- `DeviceService`：设备发现和连接管理
- `WalService`：WAL协议同步服务
- `SocketService`：WebSocket连接池管理
- `AuthService`：认证服务

**Backend（API层）**
- `api/`：封装所有后端API调用
- `schema/`：定义数据模型和序列化

### 3.2 后端服务（Python FastAPI）

#### 3.2.1 目录结构

```
backend/
├── main.py                      # FastAPI应用入口
├── routers/                     # API路由
│   ├── transcribe.py           # 转录API
│   ├── conversations.py        # 对话API
│   ├── chat.py                 # 聊天API
│   ├── memories.py             # 记忆API
│   ├── action_items.py         # 行动项API
│   ├── apps.py                 # 应用API
│   ├── plugins.py              # 插件API
│   ├── auth.py                 # 认证API
│   └── ...
├── database/                    # 数据库层
│   ├── conversations.py       # 对话数据库操作
│   ├── users.py                # 用户数据库操作
│   ├── memories.py             # 记忆数据库操作
│   ├── vector_db.py            # 向量数据库（Pinecone）
│   ├── redis_db.py             # Redis缓存
│   └── ...
├── utils/                       # 工具模块
│   ├── stt/                    # 语音转文字
│   │   ├── streaming.py        # 流式STT
│   │   ├── vad.py              # 语音活动检测
│   │   └── speech_profile.py   # 语音配置文件
│   ├── llm/                    # LLM相关
│   │   ├── chat.py             # 聊天逻辑
│   │   ├── conversation_processing.py  # 对话处理
│   │   ├── memories.py         # 记忆提取
│   │   └── clients.py           # LLM客户端
│   ├── retrieval/              # 检索模块
│   │   ├── rag.py              # RAG检索
│   │   ├── graph.py            # 图检索
│   │   └── tools/               # 检索工具
│   ├── conversations/          # 对话处理
│   │   ├── process_conversation.py
│   │   └── search.py
│   └── ...
├── models/                      # 数据模型
└── modal/                       # Modal部署配置
```

#### 3.2.2 核心模块说明

**Routers（API路由）**
- `transcribe.py`：处理实时音频流，WebSocket连接，STT调用
- `conversations.py`：对话CRUD、搜索、处理
- `chat.py`：AI聊天接口，上下文检索
- `memories.py`：记忆管理API
- `action_items.py`：行动项管理API
- `apps.py`：第三方应用管理API

**Database（数据库层）**
- `conversations.py`：Firestore对话数据操作
- `vector_db.py`：Pinecone向量数据库操作
- `redis_db.py`：Redis缓存操作（对话状态、用户数据）

**Utils（工具模块）**
- `stt/streaming.py`：流式STT处理，支持Deepgram/Soniox
- `llm/chat.py`：AI聊天逻辑，上下文构建
- `llm/conversation_processing.py`：对话后处理，结构化提取
- `retrieval/rag.py`：RAG检索，向量相似度搜索
- `conversations/process_conversation.py`：对话处理主流程

### 3.3 设备固件

#### 3.3.1 Omi设备固件（Zephyr RTOS）

```
omi/firmware/
├── omi/                        # 主固件代码
│   ├── main.c                  # 主程序
│   ├── audio/                  # 音频处理
│   ├── ble/                    # 蓝牙BLE
│   └── ...
└── devkit/                     # 开发套件固件
```

**功能：**
- 音频采集和编码（AAC/Opus/LC3）
- 蓝牙BLE通信
- 离线存储（SD卡）
- 电池管理
- 按钮和LED控制

#### 3.3.2 Omi Glass固件（ESP32-S3）

```
omiGlass/firmware/
└── ...
```

**功能：**
- 图像采集
- 蓝牙通信
- AI图像描述

### 3.4 SDK模块

#### 3.4.1 React Native SDK

```
sdks/react-native/
├── src/
│   ├── OmiConnection.ts        # 连接管理
│   ├── codecs.ts               # 编解码器
│   └── types.ts                # 类型定义
└── example/                    # 示例应用
```

#### 3.4.2 Python SDK

```
sdks/python/
├── omi/
│   ├── bluetooth.py            # 蓝牙通信
│   ├── transcribe.py           # 转录功能
│   └── decoder.py              # 音频解码
└── examples/
```

#### 3.4.3 Swift SDK

```
sdks/swift/
└── ...
```

### 3.5 插件系统

#### 3.5.1 插件架构

```
plugins/
├── example/                    # 示例插件
│   ├── basic/                 # 基础插件
│   ├── advanced/              # 高级插件
│   └── notifications/         # 通知插件
├── composio/                   # Composio集成
├── hume-ai/                    # Hume AI集成
└── apps-js/                    # JavaScript应用
```

**插件类型：**
- **Webhook插件**：接收对话事件的Webhook
- **实时插件**：实时接收音频流
- **OAuth插件**：需要OAuth认证的集成

### 3.6 Web前端

#### 3.6.1 主前端（Next.js）

```
web/frontend/
├── src/
│   ├── app/                   # Next.js App Router
│   ├── components/            # React组件
│   ├── actions/               # Server Actions
│   └── lib/                   # 工具库
└── ...
```

**功能：**
- 对话管理界面
- 记忆浏览
- 应用商店
- 设置管理

#### 3.6.2 Personas开源版

```
web/personas-open-source/
└── ...
```

**功能：**
- AI角色创建
- 社交媒体集成（Twitter等）

---

## 四、技术栈

### 4.1 前端技术栈
- **Flutter**：跨平台移动应用框架
- **Provider**：状态管理
- **Firebase**：认证、推送、Crashlytics
- **WebSocket**：实时通信
- **Bluetooth BLE**：设备通信

### 4.2 后端技术栈
- **FastAPI**：Python Web框架
- **Firebase/Firestore**：主数据库
- **Pinecone**：向量数据库
- **Redis**：缓存和实时数据
- **OpenAI/Anthropic**：LLM服务
- **Deepgram/Soniox**：STT服务
- **Modal**：云函数部署

### 4.3 设备技术栈
- **Zephyr RTOS**：Omi设备实时操作系统
- **ESP-IDF**：Omi Glass开发框架
- **nRF SDK**：Nordic芯片开发

### 4.4 基础设施
- **Docker**：容器化部署
- **Kubernetes**：容器编排（Helm Charts）
- **ngrok**：本地开发隧道
- **GitHub Actions**：CI/CD

---

## 五、数据流

### 5.1 音频数据流

```
硬件设备 → BLE → Flutter App → WebSocket → FastAPI → STT服务 → 转录文本
```

### 5.2 对话数据流

```
转录文本 → 结构化提取 → Firestore → 向量化 → Pinecone → RAG检索
```

### 5.3 用户交互流

```
用户操作 → Flutter UI → Provider → API调用 → FastAPI → 数据库 → 响应
```

---

## 六、关键特性

### 6.1 实时性
- 音频流实时传输和处理
- WebSocket双向通信
- 流式AI响应

### 6.2 离线支持
- 设备端离线存储（WAL）
- 连接时自动同步
- 断点续传

### 6.3 可扩展性
- 插件系统支持第三方集成
- 模块化架构
- 多平台SDK

### 6.4 智能化
- AI驱动的记忆提取
- 上下文感知的对话
- 智能检索和推荐

---

## 七、部署架构

### 7.1 后端部署
- **Modal**：无服务器函数部署
- **Kubernetes**：容器化服务（Deepgram自托管等）
- **Firebase**：托管数据库和认证

### 7.2 前端部署
- **移动端**：iOS App Store、Google Play
- **Web端**：Vercel/Next.js部署
- **桌面端**：macOS、Windows、Linux

### 7.3 设备固件
- **OTA更新**：通过App推送固件更新
- **DFU模式**：设备固件升级模式

---

## 八、开发流程

### 8.1 本地开发
1. 克隆仓库
2. 配置环境变量（`.env`）
3. 启动后端服务（`uvicorn main:app`）
4. 运行Flutter应用（`flutter run`）
5. 使用ngrok暴露本地后端

### 8.2 测试
- 单元测试：后端Python测试
- 集成测试：Flutter集成测试
- 设备测试：真实硬件测试

### 8.3 部署
- 代码提交到GitHub
- CI/CD自动构建和部署
- 版本管理和回滚

---

## 九、总结

Omi是一个功能完整、架构清晰的AI可穿戴设备生态系统，具有以下特点：

1. **全栈架构**：从硬件固件到移动应用、后端服务、Web前端的完整实现
2. **实时处理**：支持实时音频流处理和AI响应
3. **智能化**：集成LLM、向量检索、RAG等先进AI技术
4. **可扩展性**：插件系统和SDK支持第三方集成
5. **跨平台**：支持iOS、Android、Web、桌面多平台

该工程展现了现代AI应用的最佳实践，包括实时通信、向量检索、流式处理、插件架构等关键技术。
