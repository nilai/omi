// AI-generated START - 账户管理卡片组件 - Generated by AI
import 'package:flutter/material.dart';
import 'package:omi/pages/newsetting/personal/widgets/basic_info_card.dart';

/// 账户管理卡片组件
/// 显示账户管理相关的操作选项，如导出数据等
class AccountManagementCard extends StatelessWidget {
  // AI-generated START - 构造函数
  const AccountManagementCard({
    super.key,
    this.items,
    this.onExportDataTap,
  });
  // AI-generated END - 构造函数

  /// 账户管理项列表（可选，如果传入则使用传入的，否则使用默认生成的）
  final List<BasicInfoItem>? items;

  /// 导出数据点击回调
  final VoidCallback? onExportDataTap;

  // AI-generated START - 构建方法
  @override
  Widget build(BuildContext context) {
    // 如果传入了 items，则使用传入的；否则使用默认生成的
    final displayItems = items ??
        [
          BasicInfoItem(
            title: '导出数据',
            subtitle: '下载你的所有数据',
            icon: Icons.download_outlined,
            iconColor: const Color(0xFF81C784), // 浅绿色
            onTap: onExportDataTap,
          ),
        ];

    return Container(
      margin: const EdgeInsets.symmetric(horizontal: 16.0, vertical: 8.0),
      decoration: BoxDecoration(
        color: Colors.white,
        borderRadius: BorderRadius.circular(12.0),
      ),
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          // 标题
          _buildHeader(),
          // 列表项
          ...displayItems.asMap().entries.map((entry) {
            final index = entry.key;
            final item = entry.value;
            return _buildListItem(item, index < displayItems.length - 1);
          }).toList(),
        ],
      ),
    );
  }
  // AI-generated END - 构建方法

  // AI-generated START - 构建标题
  Widget _buildHeader() {
    return Padding(
      padding: const EdgeInsets.fromLTRB(16.0, 16.0, 16.0, 12.0),
      child: Text(
        '账户管理',
        style: const TextStyle(
          fontSize: 18.0,
          fontWeight: FontWeight.w600,
          color: Color(0xFF212121),
        ),
      ),
    );
  }
  // AI-generated END - 构建标题

  // AI-generated START - 构建列表项
  Widget _buildListItem(BasicInfoItem item, bool showDivider) {
    return Column(
      children: [
        InkWell(
          onTap: item.onTap,
          child: Padding(
            padding: const EdgeInsets.symmetric(horizontal: 16.0, vertical: 16.0),
            child: Row(
              children: [
                // 图标
                Container(
                  width: 40.0,
                  height: 40.0,
                  decoration: BoxDecoration(
                    color: item.iconColor,
                    shape: BoxShape.circle,
                  ),
                  child: Icon(
                    item.icon,
                    color: Colors.white,
                    size: 20.0,
                  ),
                ),
                const SizedBox(width: 16.0),
                // 文本内容
                Expanded(
                  child: Column(
                    crossAxisAlignment: CrossAxisAlignment.start,
                    children: [
                      Text(
                        item.title,
                        style: const TextStyle(
                          fontSize: 16.0,
                          fontWeight: FontWeight.w500,
                          color: Color(0xFF212121),
                        ),
                      ),
                      const SizedBox(height: 4.0),
                      Text(
                        item.subtitle,
                        style: TextStyle(
                          fontSize: 14.0,
                          color: Colors.grey[600],
                        ),
                      ),
                    ],
                  ),
                ),
                // 右箭头
                Icon(
                  Icons.chevron_right,
                  color: Colors.grey[400],
                  size: 20.0,
                ),
              ],
            ),
          ),
        ),
        // 分隔线
        if (showDivider)
          Divider(
            height: 1.0,
            thickness: 1.0,
            color: Colors.grey[200],
            indent: 72.0, // 对齐到文本开始位置
          ),
      ],
    );
  }
  // AI-generated END - 构建列表项
}
// AI-generated END - account_management_card.dart
