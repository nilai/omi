// AI-generated START - 自定义模版卡片组件 - Generated by AI
import 'package:flutter/material.dart';

/// 自定义模版卡片组件
/// 显示创建自定义模版的入口
class CustomTemplateCard extends StatelessWidget {
  // AI-generated START - 构造函数
  const CustomTemplateCard({
    super.key,
    this.title = '自定义模版',
    this.description = '根据你的需求创建专属的AI总结模版',
    this.buttonText = '创建模版',
    this.onCreateTap,
  });
  // AI-generated END - 构造函数

  /// 标题
  final String title;

  /// 描述文字
  final String description;

  /// 按钮文字
  final String buttonText;

  /// 创建按钮点击回调
  final VoidCallback? onCreateTap;

  // AI-generated START - 构建方法
  @override
  Widget build(BuildContext context) {
    return Container(
      margin: const EdgeInsets.symmetric(horizontal: 16.0, vertical: 8.0),
      padding: const EdgeInsets.all(24.0),
      decoration: BoxDecoration(
        color: Colors.grey[100], // 浅灰色背景
        borderRadius: BorderRadius.circular(12.0),
        border: Border.all(
          color: Colors.grey[300]!,
          width: 1.0,
          style: BorderStyle.solid, // 虚线边框
        ),
      ),
      child: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: [
          // 加号图标
          _buildAddIcon(),
          const SizedBox(height: 16.0),
          // 标题
          _buildTitle(),
          const SizedBox(height: 8.0),
          // 描述文字
          _buildDescription(),
          const SizedBox(height: 24.0),
          // 创建按钮
          _buildCreateButton(),
        ],
      ),
    );
  }
  // AI-generated END - 构建方法

  // AI-generated START - 构建加号图标
  Widget _buildAddIcon() {
    return Container(
      width: 56.0,
      height: 56.0,
      decoration: BoxDecoration(
        color: Colors.grey[300], // 深灰色背景
        borderRadius: BorderRadius.circular(12.0),
      ),
      child: const Icon(
        Icons.add,
        size: 32.0,
        color: Color(0xFF212121),
      ),
    );
  }
  // AI-generated END - 构建加号图标

  // AI-generated START - 构建标题
  Widget _buildTitle() {
    return Text(
      title,
      style: const TextStyle(
        fontSize: 18.0,
        fontWeight: FontWeight.w600,
        color: Color(0xFF212121),
      ),
    );
  }
  // AI-generated END - 构建标题

  // AI-generated START - 构建描述
  Widget _buildDescription() {
    return Text(
      description,
      textAlign: TextAlign.center,
      style: TextStyle(
        fontSize: 14.0,
        color: Colors.grey[700],
      ),
    );
  }
  // AI-generated END - 构建描述

  // AI-generated START - 构建创建按钮
  Widget _buildCreateButton() {
    return GestureDetector(
      onTap: onCreateTap,
      child: Container(
        height: 40.0,
        padding: const EdgeInsets.symmetric(horizontal: 24.0),
        decoration: BoxDecoration(
          color: Colors.white,
          borderRadius: BorderRadius.circular(20.0), // 半圆角
          border: Border.all(
            color: Colors.grey[300]!,
            width: 1.0,
          ),
        ),
          child: Column(
            mainAxisAlignment: MainAxisAlignment.center,
            children: [
               Text(
            buttonText,
            style: const TextStyle(
              fontSize: 14.0,
              fontWeight: FontWeight.w500,
              color: Color(0xFF212121),
            ),
          )
            ],
          ),
          
      ),
    );
  }
  // AI-generated END - 构建创建按钮
}
// AI-generated END - custom_template_card.dart

