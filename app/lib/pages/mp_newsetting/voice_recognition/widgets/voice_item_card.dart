// AI-generated START - 声音项卡片组件 - Generated by AI
import 'package:flutter/material.dart';
import 'package:omi/gen/assets.gen.dart';
import 'package:omi/pages/mp_newsetting/voice_recognition/widgets/my_voice_card.dart';

/// 声音项卡片组件
/// 显示单个用户录制的语音信息
class VoiceItemCard extends StatelessWidget {
  // AI-generated START - 构造函数
  const VoiceItemCard({
    super.key,
    required this.item,
    this.onTap,
    this.onActionTap,
  });
  // AI-generated END - 构造函数

  /// 声音数据项
  final MyVoiceItem item;

  /// 卡片点击回调
  final VoidCallback? onTap;

  /// 按钮点击回调
  final VoidCallback? onActionTap;

  // AI-generated START - 构建方法
  @override
  Widget build(BuildContext context) {
    return Container(
      margin: const EdgeInsets.symmetric(horizontal: 16.0, vertical: 4.0),
      decoration: BoxDecoration(
        color: Colors.white,
        borderRadius: BorderRadius.circular(12.0),
        boxShadow: [
          BoxShadow(
            color: Colors.grey.withValues(alpha: 0.1),
            spreadRadius: 1,
            blurRadius: 4,
            offset: const Offset(0, 2),
          ),
        ],
      ),
      child: InkWell(
        onTap: onTap,
        borderRadius: BorderRadius.circular(12.0),
        child: Padding(
          padding: const EdgeInsets.all(16.0),
          child: Row(
            children: [
              // 左侧图标
              _buildVoiceIcon(),
              const SizedBox(width: 16.0),
              // 中间文本信息
              Expanded(
                child: _buildContent(),
              ),
              // 右侧操作图标（编辑或删除）
              _buildActionIcon(),
            ],
          ),
        ),
      ),
    );
  }
  // AI-generated END - 构建方法

  // AI-generated START - 构建语音图标
  Widget _buildVoiceIcon() {
    //优先显示 imageUrl（网络图片）
    if (item.imageUrl != null && item.imageUrl!.isNotEmpty) {
      return Container(
        width: 60.0,
        height: 60.0,
        decoration: const BoxDecoration(
          shape: BoxShape.circle,
        ),
        child: ClipOval(
            child: Image.network(
          item.imageUrl!,
          width: 60.0,
          height: 60.0,
          fit: BoxFit.cover,
          errorBuilder: (context, error, stackTrace) {
            return _buildPlaceholder();
          },
        )),
      );
    }
    // 如果没有 imageUrl，则显示 icon（本地图片）
    if (item.icon != null && item.icon!.isNotEmpty) {
      return Container(
        width: 60.0,
        height: 60.0,
        decoration: const BoxDecoration(
          shape: BoxShape.circle,
        ),
        child: ClipOval(
          child: Image.asset(
            item.icon!,
            width: 60.0,
            height: 60.0,
            fit: BoxFit.cover,
            errorBuilder: (context, error, stackTrace) {
              return _buildPlaceholder();
            },
          ),
        ),
      );
    }
    // 如果都没有，显示占位符
    return _buildPlaceholder();
  }
  // AI-generated END - 构建语音图标

  // AI-generated START - 构建占位符
  Widget _buildPlaceholder() {
    return Container(
      width: 60.0,
      height: 60.0,
      decoration: const BoxDecoration(
        gradient: LinearGradient(
          begin: Alignment.centerLeft,
          end: Alignment.centerRight,
          colors: [
            Color(0xFF9C27B0), // 紫色
            Color(0xFFBA68C8), // 浅紫色
          ],
        ),
        shape: BoxShape.circle,
      ),
      child: const Icon(
        Icons.person,
        color: Colors.white,
        size: 28.0,
      ),
    );
  }
  // AI-generated END - 构建占位符

  // AI-generated START - 构建内容文本
  Widget _buildContent() {
    return Column(
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        // 主标题
        Text(
          item.title,
          style: const TextStyle(
            fontSize: 16.0,
            fontWeight: FontWeight.w500,
            color: Color(0xFF212121),
          ),
        ),
        const SizedBox(height: 8.0),
        // 时长和日期
        Row(
          children: [
            // 时长
            Icon(
              Icons.access_time,
              size: 14.0,
              color: Colors.grey[600],
            ),
            const SizedBox(width: 4.0),
            Text(
              item.duration,
              style: TextStyle(
                fontSize: 12.0,
                color: Colors.grey[600],
              ),
            ),
            const SizedBox(width: 16.0),
            // 日期
            Icon(
              Icons.calendar_today,
              size: 14.0,
              color: Colors.grey[600],
            ),
            const SizedBox(width: 4.0),
            Text(
              item.date,
              style: TextStyle(
                fontSize: 12.0,
                color: Colors.grey[600],
              ),
            ),
          ],
        ),
      ],
    );
  }
  // AI-generated END - 构建内容文本

  // AI-generated START - 构建操作图标
  Widget _buildActionIcon() {
    return InkWell(
      onTap: onActionTap,
      child: Assets.images.settingRightArrow1.image(width: 20.0, height: 20.0),
    );
  }
  // AI-generated END - 构建操作图标
}

// AI-generated END - voice_item_card.dart
