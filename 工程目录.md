# Omi 工程目录说明

## 一、目录设计说明

本仓库采用**多技术栈、多产品共仓**的目录布局，按业务域与技术栈划分子目录，便于设备端、服务端、前端、文档与插件统一迭代。

### 1.1 根目录概览

| 目录/文件 | 用途 |
|-----------|------|
| `app/` | 主端 Flutter 应用（移动端/桌面/Web） |
| `backend/` | Python FastAPI 后端服务 |
| `web/` | Web 前端工程（含管理后台、开源 Persona 等） |
| `docs/` | 文档站点（Mintlify） |
| `plugins/` | 插件与应用生态（JS/Python 插件、说明与素材） |
| `sdks/` | 多平台 SDK（Python / React Native / Swift） |
| `omi/` | Omi 设备固件与硬件设计（Zephyr + 3D/电路） |
| `omiGlass/` | 眼镜形态产品（Expo 应用 + 固件 + 硬件） |
| `mcp/` | MCP 服务（Python） |
| `scripts/` | 仓库级脚本（OTA、pre-commit 等） |
| `.github/workflows/` | CI/CD，按路径触发各子项目部署 |

根目录还有跨子项目使用的 `Package.swift`（指向 `sdks/swift`）、`package.json`（少量通用依赖）、`Package.resolved`、`codemagic.yaml`（Flutter 构建）等。

### 1.2 各子目录职责

#### `app/` — 主端 Flutter 应用

- **技术栈**：Flutter 3.x，Dart。
- **平台**：`android/`、`ios/`、`web/`、`macos/`、`windows/`、`linux/`。
- **主体代码**：`lib/`（约 681 个 Dart 文件）。
- **资源与配置**：`assets/`、`setup/`、各平台工程文件。
- **说明**：负责设备发现与连接、音频上行、与后端 WebSocket/API 通信、UI 与业务逻辑，是面向用户的主客户端。

#### `backend/` — 后端服务

- **技术栈**：Python，FastAPI，多子服务/任务。
- **入口**：`main.py`，`pyproject.toml` / `requirements.txt`。
- **主要子目录**：
  - `routers/`：API 路由
  - `models/`：数据与业务模型
  - `utils/`：STT、LLM、记忆、对话处理、推送等
  - `database/`：用户、任务、用量等
  - `modal/`：Modal 上的异步任务（VAD、语音档案等）
  - `pusher/`：推送服务
  - `memories-tuner/`：记忆相关调优
  - `charts/`：K8s/Helm 等部署配置
- **说明**：承担转录、对话处理、记忆检索、插件/Persona、通知、计费等核心逻辑，可独立部署（见 `.github/workflows/gcp_*.yml`）。

#### `web/` — Web 前端

- **子项目**：如 `frontend/`（Next.js 管理后台）、`personas-open-source/` 等。
- **技术**：Next.js、React、TypeScript；`frontend` 使用 `next dev --turbo`（Next 自带的 Turbopack，非 Turborepo）。
- **说明**：管理端、活动页或开源示例等 Web 产品，各自有 `package.json`，独立构建与部署。

#### `docs/` — 文档站

- **技术**：Mintlify（`@mintlify/scraping` 等），MDX 内容。
- **内容**：`doc/` 下按模块组织的文档（assembly、developer、backend、hardware、get_started 等），以及 `api-reference/`、`onboarding/` 等。
- **说明**：对外文档与 API 文档的源站，通过 `deploy_docs.yml`、`sync-docs.yml` 等单独发布。

#### `plugins/` — 插件与应用生态

- **apps-js/**：JS 类应用/插件（如 deck、email 等）。
- **composio/**、**hume-ai/**、**example/**：Python 插件或示例，含 Dockerfile/Procfile，可独立运行。
- **instructions/**：各插件/用例的说明（如 gcal、notion-crm、zapier 等）。
- **logos/**：插件/Persona 相关图片资源。
- **说明**：与后端「应用/插件」能力对应，通过 `gcp_apps_js.yml`、`gcp_plugins.yml` 等按需部署。

#### `sdks/` — 多端 SDK

- **python/**：Python SDK（BLE、转录等），`pyproject.toml`，可单独发布。
- **react-native/**：React Native SDK + example 工程。
- **swift/**：Swift 库（BLE、音频等），被根目录 `Package.swift` 引用。
- **omi-expo/**：Expo 相关占位或壳工程。
- **说明**：供第三方或姊妹项目集成设备能力，与 `app` 共享部分协议与能力定义。

#### `omi/` — 设备固件与硬件

- **firmware/**：Zephyr RTOS，nRF5340 等板级配置（`boards/omi/`），`devkit/` 与 `omi/` 等应用靶子，以及构建/烧录脚本。
- **hardware/**：3D 模型（STL/STEP）、电路图等（如 consumer、triangle v1/v2、consumer-watch-band）。
- **说明**：面向 Omi 硬件的固件与工业设计，与 `app` 通过 BLE/协议交互。

#### `omiGlass/` — 眼镜形态产品

- **应用**：Expo/React（`App.tsx`、`sources/`），含 agent、成像、设备视图等。
- **firmware/**：嵌入式固件（如 .ino/.cpp）。
- **hardware/**：镜架、外壳等 3D 与结构文件。
- **说明**：独立产品线，与主端 `app` 和 `backend` 可共享部分后端能力，但构建与发布链路独立。

#### `mcp/` — MCP 服务

- **技术**：Python，`pyproject.toml`，`src/mcp_server_omi/`。
- **说明**：提供 MCP 协议服务，供 IDE/工具链等集成，有独立 Docker 与发布流程。

#### `scripts/` — 仓库级脚本

- 如 `ota_update.py`、`pre-commit`、`requirements.txt`，用于仓库级操作（OTA、提交检查等），不绑定单一子项目。

#### `.github/workflows/` — CI/CD

- **按路径触发**：例如 `gcp_frontend.yml` 监听 `web/frontend/**`，`gcp_backend.yml` 等监听 `backend/**`，实现「改哪儿部署哪儿」。
- **其他**：`deploy_docs.yml`、`sync-docs.yml`、`gcp_apps_js.yml`、`gcp_plugins.yml`、`gcp_models.yml`、`gcp_personas.yml`、`gcp_notifications_job.yml`、`gcp_backend_pusher.yml`、`lint.yml`、`main.yml` 等，分别对应前端、后端、插件、文档、流水线等。
- **说明**：没有使用 Turborepo/Nx 的 task 图，而是用路径过滤 + 多 workflow 实现多目标构建与部署。

---

## 二、Turborepo 与 Monorepo 分析

### 2.1 结论摘要

- **仓库形态**：**Monorepo**（多个应用/库共用一个 Git 仓库）。
- **是否 Turborepo**：**否**。未使用 Turborepo 作为构建与任务编排工具。
- **补充**：`web/frontend` 中的 `next dev --turbo` 指 Next.js 的 **Turbopack** 开发模式，与 **Turborepo** 无关。

### 2.2 判断依据

| 检查项 | 本仓库情况 |
|--------|------------|
| `turbo.json` | 不存在 |
| `nx.json` | 不存在 |
| `pnpm-workspace.yaml` / `lerna.json` | 不存在 |
| 根 `package.json` 的 `workspaces` | 未定义；根 package 仅少量通用依赖（如 `expo-file-system`、`ioredis`、`dotenv-cli`） |
| 多包/多应用编排 | 各子项目自带构建系统，CI 通过路径与多 workflow 编排 |
| 统一 JS/TS 构建与缓存 | 未使用 Turborepo/Nx 的 task pipeline 与缓存 |

因此，本仓库是「**多语言、多技术栈的 Monorepo**」，而不是「基于 Turborepo 的 Monorepo」。

### 2.3 本仓库的 Monorepo 特征

1. **多产品、多目标同仓**：主端 App、后端、Web 前端、文档、多种插件、多端 SDK、固件与硬件、MCP 等都在同一 repo。
2. **技术栈异构**：Flutter(Dart)、Python、Node/Next.js、Swift、C/Zephyr 等并存，各自使用原生工具链（pubspec、pyproject/requirements、package.json、Package.swift、CMake 等）。
3. **独立构建与部署**：每个可部署单元（frontend、backend、plugins、docs 等）有自身的构建命令与镜像/发布方式，通过 GitHub Actions 的 `paths` 与多 workflow 触发，而不是通过 Turborepo 的 `turbo run build` 等统一入口。
4. **共享资产**：通过目录与引用共享协议、类型、文档和脚本（如根 `Package.swift` 引用 `sdks/swift`），但不存在 Turborepo 式的 `internal packages` 与 `dependsOn` 任务图。

### 2.4 若要引入 Turborepo 的适用场景

若将来希望用 Turborepo 做「**纯 JS/TS 部分**」的编排，可考虑：

- 将 `web/` 下多个前端工程、以及可能抽离的共享 TS 库，纳入一个 `packages/` 或 `apps/` 布局。
- 在仓库根或 `web/` 下增加 `turbo.json`，用 `dependsOn` 和 `outputs` 管理 build/lint/test 与缓存。
- 后端、Flutter、固件、硬件等仍保留现有构建与 CI 方式，Turborepo 只负责 JS/TS 子图。

当前仓库并未采用上述结构，因此维持「**Monorepo，非 Turborepo**」的结论。

---

## 三、目录与 CI 对应关系（简要）

| 目录/变更路径 | 主要 CI / 部署 |
|---------------|----------------|
| `app/` | `codemagic.yaml`、可能的主线 workflow |
| `backend/` | `gcp_backend.yml`、`gcp_backend_pusher.yml`、`gcp_models.yml`、`gcp_notifications_job.yml`、`gcp_personas.yml` 等 |
| `web/frontend/` | `gcp_frontend.yml` |
| `web/personas-open-source/` 等 | 依具体 workflow 的 paths 配置 |
| `plugins/`（如 apps-js） | `gcp_apps_js.yml`、`gcp_plugins.yml` |
| `docs/` | `deploy_docs.yml`、`sync-docs.yml` |
| 其他 | `lint.yml`、`main.yml` 等 |

以上对应关系可根据实际 `paths` 与 `workflow_dispatch` 以仓库内 `.github/workflows/*.yml` 为准。
